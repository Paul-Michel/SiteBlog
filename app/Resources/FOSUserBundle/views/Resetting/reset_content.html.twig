{% trans_default_domain 'FOSUserBundle' %}
{{ form_start(form, { 'action': path('fos_user_resetting_reset', {'token': token}), 'attr': { 'class': 'input-text' } }) }}
    {{ form_widget(form) }}
    <div>
        <input class="waves-effect waves-light btn" type="submit" value="{{ 'resetting.reset.submit'|trans }}" />
    </div>

{{ form_end(form) }}